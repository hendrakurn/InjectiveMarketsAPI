import { WireType } from "@protobuf-ts/runtime";
import { UnknownFieldHandler } from "@protobuf-ts/runtime";
import { reflectionMergePartial } from "@protobuf-ts/runtime";
import { MessageType } from "@protobuf-ts/runtime";
import { Coin } from "../../base/v1beta1/coin_pb.js";
class SendAuthorization$Type extends MessageType {
  constructor() {
    super("cosmos.bank.v1beta1.SendAuthorization", [
      { no: 1, name: "spend_limit", kind: "message", repeat: 2, T: () => Coin, options: { "gogoproto.nullable": false, "gogoproto.castrepeated": "github.com/cosmos/cosmos-sdk/types.Coins", "amino.encoding": "legacy_coins", "amino.dont_omitempty": true } },
      { no: 2, name: "allow_list", kind: "scalar", repeat: 2, T: 9, options: { "cosmos_proto.scalar": "cosmos.AddressString" } }
    ], { "cosmos_proto.implements_interface": ["cosmos.authz.v1beta1.Authorization"], "amino.name": "cosmos-sdk/SendAuthorization" });
  }
  create(value) {
    const message = globalThis.Object.create(this.messagePrototype);
    message.spendLimit = [];
    message.allowList = [];
    if (value !== void 0)
      reflectionMergePartial(this, message, value);
    return message;
  }
  internalBinaryRead(reader, length, options, target) {
    let message = target ?? this.create(), end = reader.pos + length;
    while (reader.pos < end) {
      let [fieldNo, wireType] = reader.tag();
      switch (fieldNo) {
        case /* repeated cosmos.base.v1beta1.Coin spend_limit */
        1:
          message.spendLimit.push(Coin.internalBinaryRead(reader, reader.uint32(), options));
          break;
        case /* repeated string allow_list */
        2:
          message.allowList.push(reader.string());
          break;
        default:
          let u = options.readUnknownField;
          if (u === "throw")
            throw new globalThis.Error(`Unknown field ${fieldNo} (wire type ${wireType}) for ${this.typeName}`);
          let d = reader.skip(wireType);
          if (u !== false)
            (u === true ? UnknownFieldHandler.onRead : u)(this.typeName, message, fieldNo, wireType, d);
      }
    }
    return message;
  }
  internalBinaryWrite(message, writer, options) {
    for (let i = 0; i < message.spendLimit.length; i++)
      Coin.internalBinaryWrite(message.spendLimit[i], writer.tag(1, WireType.LengthDelimited).fork(), options).join();
    for (let i = 0; i < message.allowList.length; i++)
      writer.tag(2, WireType.LengthDelimited).string(message.allowList[i]);
    let u = options.writeUnknownFields;
    if (u !== false)
      (u == true ? UnknownFieldHandler.onWrite : u)(this.typeName, message, writer);
    return writer;
  }
}
const SendAuthorization = new SendAuthorization$Type();
export {
  SendAuthorization
};
